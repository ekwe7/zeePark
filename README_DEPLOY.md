# ZeePark Backend

This is the standalone backend service for ZeePark.

## Deployment

To deploy the backend (including MongoDB):

1.  Configure your environment in `.env`.
2.  Run with Docker Compose:

```bash
docker-compose up -d --build
```

The API will be available at `http://localhost:8181`.
